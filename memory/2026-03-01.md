# Diário — 2026-03-01

## LIN-001 — Sistema de Integração Linear App

### Fluxo executado
- Lincoln pediu integração com Linear App
- Arquiteto levantou pontos faltantes (estrutura, one-way vs two-way, permissões, estados, notificações, KeyMaker no escopo)
- Lincoln respondeu todas as decisões
- Ticket aberto com Oráculo para PRD
- Oráculo entregou PRD em `workspaces/oraculo/memory/plans/2026-03-01-prd-linear-integration.md`
- Lincoln aprovou o PRD
- Arquiteto delegou para KeyMaker executar

### Problema: KeyMaker falhou em materializar artefatos
- KeyMaker respondeu ETAs e status mas não criou nenhum arquivo no disco
- Após 3 tentativas (16:45, 17:00, 19:00), Arquiteto assumiu a implementação diretamente
- **Lição:** KeyMaker (gpt-5.3-codex) tem dificuldade em tarefas multi-passo com filesystem — tende a responder planos em vez de executar. Precisa de guardrail: timeout de 20min → Arquiteto assume automaticamente.

### O que foi implementado (pelo Arquiteto)
- `/home/lincoln/.openclaw/workspace/scripts/linear-cli.js` — CLI Node.js centralizado
- `/home/lincoln/.openclaw/workspace/scripts/package.json` — @linear/sdk
- teamKey: LIN (não SQD — workspace do Lincoln)
- Estados criados via API: **Em Testes** + **Entregue**
- Labels criadas via API: agent:arquiteto/postmaster/oraculo/keymaker + type:feature/bug/infra/research/automation
- Bug corrigido: update/comment usam searchIssues (API não aceita filter por identifier)
- Ticket LIN-5 criado e marcado como Entregue no Linear

### Commit
- `d89eb7f` — feat(linear): add linear-cli.js — CLI centralizado do esquadrão

### Segurança
- Chave lin_api_* ficou exposta no Telegram (histórico de conversa) — Lincoln foi orientado a rotacionar
- ~/.linear-token criado com chmod 600

---

## Contexto técnico registrado

### teamKey real do workspace do Lincoln
- Não é SQD — é **LIN** (Lincoln Squad)

### Bug @linear/sdk
- `client.issues({ filter: { identifier: { eq: "LIN-5" } } })` retorna erro — "Field 'identifier' is not defined by type IssueFilter"
- Workaround: `client.searchIssues(identifier)` + `.find(i => i.identifier === key)`

---

## Chave de API exposta — ✅ RESOLVIDO
- Chave Linear original exposta no histórico do Telegram
- Rotacionada no painel do Linear às ~19:57 pelo Lincoln
- ~/.linear-token atualizado com a nova chave (confirmado 17:03)
- GitHub Push Protection bloqueou commit com a chave antiga — removida antes do push
